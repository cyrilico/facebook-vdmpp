class TestPublication is subclass of Test

instance variables
	
operations
	protected Setup: () ==> ()
			Setup() == is not yet specified;
			
	public Run: () ==> ()
			Run() == (
				IO`println("\nRunning Publication Tests\n");
								
					
				IO`println("\nPublication Tests ran successfully\n");
			);

end TestPublication